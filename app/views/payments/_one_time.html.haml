%script{:src => "https://checkout.stripe.com/checkout.js", :type => "text/javascript"}
= form_tag payments_path do
  #error_explanation
    - if flash[:error].present?
      %p= flash[:error]
  %article
    = label_tag(:amount, 'Amount:')
    = text_field_tag(:amount)
  %article= hidden_field_tag(:stripeToken)
  %article= hidden_field_tag(:description)
  %button#one_time_pay One Time Pay

:javascript
  var key = "#{Rails.configuration.stripe[:publishable_key]}"
  var email = "#{ current_user ? current_user.email : current_sponsorer.email }"